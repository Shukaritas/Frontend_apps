/**
 * @typedef {'Healthy' | 'Attention' | 'Critical'} CropStatus
 *
 * Representa el Modelo de un Campo con la estructura de datos ideal
 * que la aplicación necesita para funcionar.
 * @typedef {object} FieldModel
 * @property {number} id
 * @property {string} name
 * @property {string} imageUrl
 * @property {string} cropName
 * @property {number} days
 * @property {CropStatus} status
 */

export {};