/**
 * @typedef {object} PreviewFieldDTO
 * @property {number} id
 * @property {string} image_url
 * @property {string} title
 */

/**
 * @typedef {object} CropStatusDTO
 * @property {number} id
 * @property {'Healthy' | 'Attention' | 'Critical'} status
 */

export {};